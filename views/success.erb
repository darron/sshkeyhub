<h1>Connecting <a href="mailto:<%= @user['email'] %>"><%= @user['email'] %></a> to <a href="<%= @user['html_url'] %>"><%= @user['login'] %></a></h1>
<img src="<%= @user['avatar_url'] %>" />

<p>Your keys are:</p>
<% @keys.each do |k,v| %>
<p><strong>MD5 Fingerprint:</strong> <%= k %><br/>
<strong>Key:</strong> <%= v %></p>
<% end %>